# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @texmecom/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t texmecom-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system texmecom-auth-service && \
          adduser --system -G texmecom-auth-service texmecom-auth-service

COPY apps/auth-service/dist texmecom-auth-service/
COPY apps/auth-service/package.json texmecom-auth-service/
RUN chown -R texmecom-auth-service:texmecom-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix texmecom-auth-service --omit=dev -f install

CMD [ "node", "texmecom-auth-service" ]
